CXX_STD = CXX14

 UNAME_S := $(shell uname -s)
 ifeq ($(UNAME_S),Darwin)
 SDKROOT := $(shell xcrun --show-sdk-path 2>/dev/null)
 ifneq ($(SDKROOT),)
 ifneq ($(wildcard $(SDKROOT)/usr/include/c++/v1/cmath),)
 PKG_CPPFLAGS += -isystem $(SDKROOT)/usr/include/c++/v1
 endif
 endif
 endif
